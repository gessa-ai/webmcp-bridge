# Repository agent instructions

## GitHub identity

- Human-authored commits and annotated tags must use `KA` as `user.name`.
- Use `25724612+ka-rar@users.noreply.github.com` as `user.email`.
- The corresponding GitHub login is `ka-rar`.
- Keep the noreply address so a personal email is never published.
- GitHub Actions may author automated releases and deployments as
  `github-actions[bot]`; do not relabel a human-authored commit as automation.
